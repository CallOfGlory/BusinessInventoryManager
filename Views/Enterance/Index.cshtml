<form method="post">
    <style>
        .btn-animated {
            transition: 0.25s ease;
        }
        .btn-animated:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(0,0,0,0.12);
        }
    </style>

    <div class="d-flex gap-3 mt-3">

        <button class="btn btn-primary flex-fill py-2 fw-semibold btn-animated"
                style="border-radius: 10px;">
            Увійти
        </button>

        <button class="btn btn-outline-primary flex-fill py-2 fw-semibold btn-animated"
                style="border-radius: 10px;">
            Реєстрація
        </button>
    </div>
</form>